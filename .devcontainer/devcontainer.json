{
  "name": "opensomeip on S-CORE devcontainer",
  "image": "ghcr.io/eclipse-score/devcontainer:latest",
  "remoteUser": "vscode",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "llvm-vs-code-extensions.vscode-clangd",
        "twxs.cmake"
      ]
    }
  },

  "postCreateCommand": "set -e; if [ ! -d opensomeip ]; then git clone https://github.com/vtz/opensomeip.git; fi; cmake -S opensomeip -B opensomeip/build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=OFF; cmake --build opensomeip/build -j"
}
